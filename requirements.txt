# Meeting Transcriber - Dependencies
# Install: pip install -r requirements.txt

# ==============================================================================
# CORE DEPENDENCIES (required)
# ==============================================================================

# WhisperX - Transcription + Diarization (default backend: --mode meeting)
# Note: whisperX installs faster-whisper, torch, torchaudio automatically
whisperx==3.7.4

# Environment variables
python-dotenv>=1.0.0

# ==============================================================================
# OPTIONAL BACKENDS
# ==============================================================================

# Fast mode (--mode fast) - Apple Silicon optimized
# Uncomment to install:
# mlx-whisper>=0.4.0

# Precise mode (--mode precise) - IBM Granite Speech
# Requires ~16GB RAM. Uncomment to install:
# transformers>=4.40.0
# accelerate>=0.30.0

# ==============================================================================
# DEVELOPMENT
# ==============================================================================

pytest>=8.0.0

# Code quality (optional):
# black>=24.0.0
# ruff>=0.4.0

# ==============================================================================
# NOTES
# ==============================================================================

# Mac Apple Silicon - if torch issues occur:
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cpu

# Install all optional backends:
#   pip install mlx-whisper transformers accelerate

# Tested with Python 3.12 (3.14 not compatible)
